PROMPT="%F{2}[%D{%F %T}] %F{5}%n@%m %F{6}%~ %F{15}%# "
alias ls='ls -G'
alias ll='ls -alG'

## Colorize the grep command output for ease of use (good for log files)##
alias grep='grep --color=auto'
alias egrep='egrep --color=auto'
alias fgrep='fgrep --color=auto'

# confirmation #
alias rm='rm -i'
alias mv='mv -i'
alias cp='cp -i'
alias ln='ln -i'

